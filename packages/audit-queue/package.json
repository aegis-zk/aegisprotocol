{
  "name": "@aegisaudit/audit-queue",
  "version": "0.1.0",
  "description": "Automated audit pipeline for AEGIS Protocol — watches for new skills, runs checklist audits, generates ZK proofs, submits on-chain attestations",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "aegis-audit-queue": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "start": "node dist/index.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aegisaudit/sdk": "workspace:*",
    "sql.js": "^1.12.0",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "tsup": "^8.3.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
